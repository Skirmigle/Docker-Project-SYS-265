Once the files are installed, be sure to change all passwords in the .env file (POSTGRES_PASSWORD, ADMIN_PASSWORD, etc.).

1. After all files have been installed, enter `docker-compose up` This should start the webserver if docker and docker-compose are installed properly.

2. next join the website via `https://127.0.0.1:8000/todos`.
